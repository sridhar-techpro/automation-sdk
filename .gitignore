node_modules/
dist/
dist-scripts/
*.js.map
*.d.ts.map
# Validation run bundles are generated artifacts.
# The latest run directory is force-added via `git add -f` so reviewers
# can see current test results without checking in every run.
validation/run-*/
.env
backend/
validation/*
validation/**/*
logs/